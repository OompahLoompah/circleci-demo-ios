version: 2
jobs:

  adhoc:
    macos:
      xcode: "15.0.0"
    working_directory: /Users/distiller/project
    environment:
      FL_OUTPUT_DIR: output
      FASTLANE_LANE: adhoc
    shell: /bin/bash --login -o pipefail
    steps:
      - checkout
      - run: time git clone git@github.com:torvalds/linux

workflows:
  version: 2
  build-test-adhoc:
    jobs:
      - adhoc
      #- beta:
          #filters:
            #branches:
              #only: master
          #requires:
            #- build-and-test
